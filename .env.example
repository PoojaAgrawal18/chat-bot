# Backend API base URL (no trailing slash).
# In dev, leave empty to use Vite proxy to localhost.
# For production or Render backend, set to your backend host:
VITE_API_BASE_URL=https://sale-dashboard-backend.onrender.com
